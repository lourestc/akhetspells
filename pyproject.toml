[tool.poetry]
name = "akhetspells"
version = "0.1.0"
description = "D&D/Pathfinder spell library manager"
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
lxml = "*"
pandas = "*"
jupyter = "*"
streamlit = ">=1.35"

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
